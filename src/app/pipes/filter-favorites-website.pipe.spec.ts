import { FilterFavoriteWebsitePipe } from './filter-favorite-website.pipe';

describe('FilterFavoriteWebsitePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFavoriteWebsitePipe();
    expect(pipe).toBeTruthy();
  });
});
